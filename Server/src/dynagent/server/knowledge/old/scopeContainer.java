package dynagent.knowledge.old;
/*package dynagent.knowledge;


public abstract class scopeContainer{
	// usada para permitir recuperar un access de contexto ignorando 
	// la operación, como proxy de la clase Contexto

	access m_access;


	public scopeContainer( access myAccess ){
		m_access= myAccess;
	}

	public scopeContainer( metaData md, Integer ctxId, Integer userRol ){
		Contexto ctx= md.getContext( ctxId );
		m_access= ctx.getAcceso( userRol );
	}

	public access getAccess(){
		return (access)m_access.clone();
	}
}*/